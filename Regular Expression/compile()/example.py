import re
S="We don't want class on Sunday"
print(re.compile('t'))              # compile(): This method is used to compile a regular expression pattern into a regular expression object.
print(re.compile('t').findall(S)) 