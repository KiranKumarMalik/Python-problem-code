# Write a Python program that takes two strings as input and concatenates them into a single string without using the `+` operator.

S=input("Enter the first string: ")
K=input("Enter the second string: ")

print(S+K)